This should be used extensively.

Each developer must create their own config apart from one config each for UAT and Production.
To use the config file set the NODE_ENV variable to the name of the json file (E.g. to use the file sudhir.office.json as the config file, set the NODE_ENV variable to "sudhir.office" <<without quotes>>)